--- Additional Changes (Current Session) ---

I. Files Modified and Improved:

   * `.htaccess`
       * Added: `RewriteRule ^(google_callback|github_callback)\.php$ - [L]` at the top to exclude OAuth callback files from all rewrite rules.

   * `router.php`
       * Updated include paths for `products.php`, `single-article.php`, `single-category.php`, `single-ad.php`, and `articles.php` to point to their new locations within `/app/pages/`.

   * `app/pages/ad-form.php`
       * Updated internal `include` and `require_once` paths to correctly reference `config/config.php`, `config/functions.php`, `partials/header.php`, and `partials/footer.php` from its new location.

   * `app/pages/Blog-form.php`
       * Updated internal `include_once` paths to correctly reference `config/config.php`, `config/functions.php`, `partials/header.php`, and `partials/footer.php` from its new location.

   * `app/pages/single-article.php`
       * Updated internal `include_once` paths to correctly reference `config/config.php`, `config/functions.php`, `partials/header.php`, and `partials/footer.php` from its new location.

   * `app/pages/single-category.php`
       * Updated internal `include_once` paths to correctly reference `config/config.php` and `partials/header.php` from its new location.

   * `app/pages/single-ad.php`
       * Updated internal `include_once` paths to correctly reference `config/config.php`, `partials/header.php`, and `partials/footer.php` from its new location.
       * Corrected AJAX call URLs for `submit_review_ajax.php` and `load_more_reviews.php` to use `$base_url`.

   * `app/pages/articles.php`
       * Updated internal `include_once` paths to correctly reference `config/config.php`, `config/functions.php`, `partials/header.php`, and `partials/footer.php` from its new location.

   * `ajax/load-subcat-posts.php`
       * Added `include_once('../config/functions.php');` to ensure `render_ads_from_database` is available.

II. Files Moved:

   * `products.php` -> `/app/pages/products.php`
   * `ad-form.php` -> `/app/pages/ad-form.php`
   * `Blog-form.php` -> `/app/pages/Blog-form.php`
   * `single-article.php` -> `/app/pages/single-article.php`
   * `single-category.php` -> `/app/pages/single-category.php`
   * `single-ad.php` -> `/app/pages/single-ad.php`
   * `articles.php` -> `/app/pages/articles.php`